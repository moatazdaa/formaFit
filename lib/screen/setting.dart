
import 'package:flutter/material.dart';

// صفحة خاصة بالاعدادات 

class SettingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('setting Screen'),
      ),
      body:
    Center(child: Text("data"),)
    );
  }
}