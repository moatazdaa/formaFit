import 'package:flutter/material.dart';

class TrainScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Train Screen"),
      ),
      body: Center(
        child: Text("Training details will be here."),
      ),
    );
  }
}